# STUDENT MANAGEMENT SYSTEM — MASTER DEVELOPMENT PROMPT

You are a senior Java Spring Boot full-stack engineer.

I want you to build a complete, production-quality **Student Management System** using the exact technology stack and requirements below.

IMPORTANT: Do NOT replace the backend technology with Node.js, Express.js, Python, Django, Firebase, Supabase, MongoDB, or any other alternative.

The backend MUST be:

* Java 21
* Spring Boot
* Maven
* Spring Web
* Spring Data JPA
* Hibernate
* MySQL
* REST APIs

The frontend UI has already been designed in **Google Stitch** and exists in this project. Treat the existing Stitch UI as the visual source of truth.

Do NOT redesign the frontend unless explicitly asked.

---

# 1. PROJECT OBJECTIVE

Build a modern Student Management System that allows administrators/staff to manage students, departments, courses, attendance, academic information, and other student-related information through a clean dashboard.

The system should have:

* Authentication
* Dashboard
* Student management
* Student details
* Add student
* Edit student
* Delete student
* Search students
* Filter students
* Department management
* Course management
* Attendance management
* Academic information
* Profile management
* Responsive UI
* REST API backend
* MySQL database

The architecture must be clean, modular, maintainable, and suitable for a final-year/college software project.

---

# 2. TECHNOLOGY STACK

## Backend

Use ONLY:

* Java 21
* Spring Boot 3.x compatible with Java 21
* Maven
* Spring Web
* Spring Data JPA
* Hibernate
* MySQL Driver
* Spring Validation
* Spring Security for authentication/authorization
* Lombok only where it improves readability

Do NOT use:

* Node.js
* Express.js
* MongoDB
* Firebase
* Supabase
* Django
* Flask
* PHP
* Laravel

---

# 3. DATABASE

Use MySQL.

Database name:

student_management_system

Use the following initial database structure as a starting point, but improve it where necessary.

## users

Fields:

* id
* username
* email
* password
* role
* created_at
* updated_at

Roles:

* ADMIN
* STAFF

## students

Fields:

* id
* student_id
* first_name
* last_name
* email
* phone
* date_of_birth
* gender
* address
* city
* state
* pincode
* department_id
* course_id
* semester
* admission_year
* profile_image
* status
* created_at
* updated_at

Status:

* ACTIVE
* INACTIVE
* GRADUATED

## departments

Fields:

* id
* name
* code
* description

## courses

Fields:

* id
* name
* code
* duration
* department_id
* description

## attendance

Fields:

* id
* student_id
* date
* status
* remarks

Attendance status:

* PRESENT
* ABSENT
* LATE
* LEAVE

Design proper relationships using JPA.

---

# 4. ENTITY RELATIONSHIPS

Implement proper JPA relationships.

Expected relationships:

Department:

One Department → Many Students

Department:

One Department → Many Courses

Course:

One Course → Many Students

Student:

One Student → Many Attendance Records

Use:

@OneToMany

@ManyToOne

where appropriate.

Avoid unnecessary bidirectional relationships if they create JSON serialization problems.

Use DTOs where appropriate instead of exposing entities directly through REST APIs.

---

# 5. BACKEND ARCHITECTURE

Use a clean layered architecture:

src/main/java/.../

├── config
├── controller
├── dto
├── entity
├── repository
├── service
├── service/impl
├── exception
├── security
└── util

Follow this flow:

Controller
↓
Service
↓
Repository
↓
Database

Do NOT put business logic inside controllers.

Controllers should be thin.

Services should contain business logic.

Repositories should handle database operations.

---

# 6. STUDENT API

Create REST APIs for students.

## Get all students

GET

/api/students

Support:

* pagination
* searching
* filtering

Example:

GET /api/students?page=0&size=10

Example:

GET /api/students?search=Anmol

---

## Get student by ID

GET

/api/students/{id}

---

## Create student

POST

/api/students

Accept JSON.

Validate:

* required name
* valid email
* valid phone
* valid department
* valid course
* valid semester

Return appropriate HTTP status codes.

---

## Update student

PUT

/api/students/{id}

---

## Delete student

DELETE

/api/students/{id}

Prefer safe deletion where appropriate.

---

# 7. DEPARTMENT API

Create:

GET /api/departments

GET /api/departments/{id}

POST /api/departments

PUT /api/departments/{id}

DELETE /api/departments/{id}

---

# 8. COURSE API

Create:

GET /api/courses

GET /api/courses/{id}

POST /api/courses

PUT /api/courses/{id}

DELETE /api/courses/{id}

Support filtering courses by department.

Example:

GET /api/courses?departmentId=1

---

# 9. ATTENDANCE API

Create:

GET /api/attendance/student/{studentId}

POST /api/attendance

PUT /api/attendance/{id}

DELETE /api/attendance/{id}

Also create an API that calculates attendance percentage.

Example:

GET /api/attendance/student/{studentId}/summary

Return something similar to:

{
"totalClasses": 100,
"present": 85,
"absent": 10,
"late": 5,
"attendancePercentage": 85.0
}

---

# 10. DASHBOARD API

Create a dashboard endpoint:

GET

/api/dashboard/summary

Return useful statistics:

{
"totalStudents": 500,
"activeStudents": 480,
"inactiveStudents": 20,
"totalDepartments": 8,
"totalCourses": 20,
"averageAttendance": 87.5
}

The frontend dashboard should consume this API instead of using hardcoded numbers.

---

# 11. AUTHENTICATION

Implement authentication using Spring Security.

Use:

* username/email
* password
* role-based authorization

Roles:

ADMIN
STAFF

Passwords MUST NOT be stored as plain text.

Use BCryptPasswordEncoder.

Implement proper authentication.

If JWT is used, structure it cleanly:

Login:

POST /api/auth/login

Response:

{
"token": "...",
"user": {
"id": 1,
"username": "admin",
"role": "ADMIN"
}
}

Protect management APIs using authentication.

ADMIN should have full access.

STAFF should have appropriate limited management access.

Do not hardcode authentication inside controllers.

---

# 12. VALIDATION

Use Jakarta Bean Validation.

Examples:

@NotBlank

@NotNull

@Email

@Size

@Pattern

Validate incoming DTOs.

Return clean validation errors.

Example:

{
"timestamp": "...",
"status": 400,
"message": "Validation failed",
"errors": {
"email": "Invalid email address"
}
}

---

# 13. EXCEPTION HANDLING

Create global exception handling using:

@RestControllerAdvice

Handle:

* ResourceNotFoundException
* DuplicateResourceException
* ValidationException
* Authentication errors
* General exceptions

Return meaningful HTTP status codes.

Do NOT expose stack traces to the frontend.

---

# 14. FRONTEND

IMPORTANT:

The frontend UI has already been designed using Google Stitch.

FIRST inspect the existing frontend files.

Understand:

* pages
* components
* navigation
* forms
* tables
* cards
* colors
* typography
* spacing
* icons
* responsive behavior

DO NOT redesign the UI.

DO NOT replace the Stitch design with a generic dashboard.

Preserve the existing visual language.

Only modify frontend code when necessary to:

1. Connect it to backend APIs.
2. Replace mock data.
3. Add missing functionality.
4. Fix bugs.
5. Add loading/error states.

---

# 15. FRONTEND API INTEGRATION

Replace hardcoded/mock student data with actual API calls.

Example:

Dashboard:

GET /api/dashboard/summary

Student list:

GET /api/students

Student creation:

POST /api/students

Student update:

PUT /api/students/{id}

Student deletion:

DELETE /api/students/{id}

Department:

GET /api/departments

Course:

GET /api/courses

Attendance:

GET /api/attendance/student/{studentId}

Authentication:

POST /api/auth/login

Create a clean API service layer in the frontend.

Do not scatter fetch/axios calls throughout every component.

---

# 16. FRONTEND STATES

Every API-driven page must handle:

* Loading state
* Success state
* Empty state
* Error state

For example:

Loading students...

No students found.

Failed to load students.

Do not leave blank screens when APIs fail.

---

# 17. SEARCH AND FILTERING

Student list should support:

Search by:

* student ID
* name
* email

Filter by:

* department
* course
* semester
* status

Implement pagination.

Do not load thousands of records unnecessarily if pagination can be performed by the backend.

---

# 18. DATABASE CONFIGURATION

Create application configuration using environment variables.

Example:

spring.datasource.url=jdbc:mysql://localhost:3306/student_management_system

Do NOT hardcode real passwords.

Use:

DB_USERNAME

DB_PASSWORD

Environment variables where appropriate.

Create a safe example configuration file if necessary.

---

# 19. CORS

Configure CORS so the Stitch frontend can communicate with Spring Boot during development.

Allow the local frontend development origin.

Do not use:

allowedOrigins("*")

in a production-style security configuration.

Make the allowed frontend origin configurable.

---

# 20. API RESPONSE DESIGN

Use consistent API responses.

Example successful response:

{
"success": true,
"message": "Student created successfully",
"data": {...}
}

Example error:

{
"success": false,
"message": "Student not found",
"data": null
}

Keep API responses predictable.

---

# 21. DATABASE SEEDING

Create initial seed data for development.

Create:

* 1 ADMIN user
* 1 STAFF user
* sample departments
* sample courses
* sample students
* sample attendance records

Passwords must be securely hashed.

Clearly document development credentials in the README only if safe to do so.

---

# 22. SECURITY REQUIREMENTS

Implement:

* BCrypt password hashing
* Authentication
* Authorization
* Input validation
* SQL injection protection through JPA
* Proper CORS configuration
* No passwords in source code
* No sensitive credentials committed to Git

Never return password fields in API responses.

---

# 23. CODE QUALITY

Follow:

* SOLID principles
* Clean code
* Meaningful naming
* Small methods
* Proper separation of concerns
* Constructor injection
* DTO-based API design
* Avoid duplicated code
* Avoid unnecessary abstractions

Do NOT generate unnecessarily complicated enterprise architecture.

This is a college project, so the code should be:

Professional
Understandable
Maintainable
Easy to explain during a viva

---

# 24. DOCUMENTATION

Create a detailed README.md containing:

1. Project overview
2. Features
3. Technology stack
4. Project architecture
5. Database setup
6. MySQL configuration
7. Environment variables
8. How to run backend
9. How to run frontend
10. API endpoints
11. Database schema
12. Authentication
13. Default development credentials
14. Screenshots section placeholder
15. Future improvements

---

# 25. DEVELOPMENT RULES

VERY IMPORTANT:

Do not blindly generate the entire application in one step.

FIRST:

1. Inspect the existing project.
2. Inspect the Stitch frontend.
3. Identify the existing frontend technology.
4. Identify existing files.
5. Identify existing components/pages.
6. Identify existing mock data.
7. Identify existing API calls.
8. Determine what is missing.

Then give me a concise implementation plan.

After I approve the plan, implement the project incrementally.

---

# 26. IMPLEMENTATION ORDER

Follow this order:

PHASE 1
Project structure and Spring Boot setup.

PHASE 2
MySQL database connection.

PHASE 3
Entities and JPA relationships.

PHASE 4
Repositories.

PHASE 5
DTOs.

PHASE 6
Services.

PHASE 7
REST Controllers.

PHASE 8
Global exception handling and validation.

PHASE 9
Authentication and Spring Security.

PHASE 10
Dashboard APIs.

PHASE 11
Frontend API integration.

PHASE 12
Student CRUD.

PHASE 13
Department and course management.

PHASE 14
Attendance management.

PHASE 15
Testing and bug fixing.

PHASE 16
Final UI polish and responsive testing.

PHASE 17
Documentation.

---

# 27. TESTING

Create tests for important backend functionality.

At minimum test:

* Student creation
* Student retrieval
* Student update
* Student deletion
* Student not found
* Department creation
* Course creation
* Attendance calculation
* Authentication
* Validation

Use JUnit and Spring Boot testing tools.

---

# 28. GIT

Create logical commits.

Examples:

feat: initialize spring boot backend

feat: add student entity and repository

feat: implement student CRUD APIs

feat: add authentication

feat: connect frontend to student APIs

fix: resolve student validation issue

Do not make one giant commit for the entire project.

---

# 29. IMPORTANT RESTRICTIONS

NEVER:

* Replace Spring Boot with Node.js.
* Replace MySQL with MongoDB.
* Replace MySQL with Firebase.
* Replace the Stitch UI.
* Create fake APIs.
* Keep mock data after backend integration.
* Hardcode database credentials.
* Store plain-text passwords.
* Put business logic inside controllers.
* Return JPA entities blindly if it causes serialization problems.
* Delete existing UI components without understanding them.
* Introduce unnecessary technologies.

If you believe a technology change is necessary, STOP and explain why before making the change.

---

# 30. FIRST TASK

Do NOT start writing hundreds of files immediately.

Your FIRST response should:

1. Inspect the repository.
2. Identify the frontend technology and Stitch-generated files.
3. Identify whether a Spring Boot backend already exists.
4. Identify the current database configuration.
5. Identify existing pages/components.
6. Identify mock data.
7. Identify missing backend functionality.
8. Propose the final folder structure.
9. Propose the database schema.
10. Propose the implementation phases.

Then WAIT for my approval.

Do not make destructive changes.

Do not delete existing files.

Do not overwrite the Stitch frontend design.

The goal is to turn the existing Stitch UI into a fully functional Student Management System backed by Java Spring Boot + MySQL.
